# Explorer Log 3 — Existing email/mail code search

Date: 2026-07-22
Dispatched by: forge-design (mail-settings)

## Question (verbatim)
"Is there any existing email/SMTP/nodemailer/mail-sending code or notification mechanism anywhere in the repo?"

## Key observations (file:line)
- NO email-sending code, NO declared mail dependency. `backend/pom.xml:34-75` has no `quarkus-mailer` / `jakarta.mail`.
- angus-mail + jakarta.mail-api present only as TRANSITIVE build artifacts under `backend/target/quarkus-app/lib/main/` (via keycloak-admin-client/RESTEasy), not used by app code, not declared.
- `application.properties`: no `quarkus.mailer.*` / SMTP settings.
- Recipient emails exist as dynamic custom fields: seed `migration/FieldDefinitionSeedMigration.java:70-73` (field key "email", schema string/format:email), stored as `FieldInstance` on persons. Consumed only by `resource/PersonResource.java:215-232` → `security/KeycloakUserService.createUser()` (`:27-42`) for Keycloak, never mailed.
- No scheduler/queue/async: no `quarkus-scheduler`/`quartz`, no `@Scheduled`/`@Async`. Only startup migrations.

## Condensed answer
Greenfield for sending: must add a mail dependency (declare angus-mail/jakarta.mail explicitly or quarkus-mailer), build SMTP send from settings. Recipient email data already available via `email` FieldInstance. No templating (Qute available but undeclared).
